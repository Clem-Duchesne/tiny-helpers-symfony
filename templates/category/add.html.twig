{% extends 'base.html.twig' %}
{% block title %}Ajouter une catégorie{% endblock %}

{% block body %}
<div class="container">
    <h1 class="title">Ajouter une catégorie</h1>
    <div class="form">
        {{ form_start(form) }}
        {{ form_row(form.name) }}
        <button class="form__button" formnovalidate>Enregistrer la catégorie</button>
        {{ form_end(form) }}
        <a href="{{ path( 'category' ) }}" class="">Annuler</a>
    </div>
</div>


{% endblock %}