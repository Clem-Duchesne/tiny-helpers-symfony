{% extends 'base.html.twig' %}
{% block title %}Ajouter une catégorie{% endblock %}

{% block body %}
<div class="container mt-4">
<h1>Ajouter une catégorie</h1>
{{ form_start(form) }}
<div class="row">
    <div class="col-md-6">
        {{ form_row(form.name) }}
        {{ form_row(form.slug) }}
    </div>
</div>
<button class="btn btn-primary" formnovalidate>Enregistrer la catégorie</button>
<a href="{{ path( 'category' ) }}" class="">Annuler</a>
{{ form_end(form) }}

</div><!-- /.container -->
{% endblock %}